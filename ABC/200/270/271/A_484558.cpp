#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
int n;

int main() {
	cin.tie(0);
	cout.tie(0);
	ios::sync_with_stdio(false);


	scanf("%d", &n);
	printf("%02X", n);

	return 0;
}
